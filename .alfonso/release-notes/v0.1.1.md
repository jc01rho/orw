# @cortexkit/orw v0.1.1

Small CLI polish release.

## Fixes

- Added `--help`, `-h`, and `help` command support.
- Unknown commands now print usage help and exit with status `1` instead of showing a Bun stack trace.
- Documented the help command in the README command list.
